<Window x:Class="DogsTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DogsTracker"
        xmlns:VM="clr-namespace:DogsTracker.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <VM:MainWindowViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="RefreshButton" Content="Refresh" Grid.Row="1"/>
        <Button x:Name="SettingsButton" Content="Settings" Grid.Row="2"/>
        <Button x:Name="HelpButton" Style="{DynamicResource ResourceKey= MenuButton}" Content="Help" Grid.Row="3"/>
        <Button x:Name="DonateButton" Content="Donate" Grid.Row="4"/>
        <Button x:Name="ExitButton" Content="Exit" Grid.Row="5"/>
        <DataGrid ItemsSource="{Binding Odds}" Grid.Column="1" Grid.RowSpan="7" x:Name="OddsList" />
    </Grid>
    <Window.Resources>
        <Style x:Key="MenuButton" TargetType="Button">
            <Setter Property="Background" Value="Lime"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>
</Window>
